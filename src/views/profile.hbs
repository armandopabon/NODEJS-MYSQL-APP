<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="car-body">
                    <h3>Welcome {{user.fullname}}</h3>
                    <h4 class="subtittle">{{user.username}}</h4>
                    <a href="/links" class="btn btn-primary">
                    Save your links
                    </a>
                </div>
            </div>
        </div>
    </div>

</div>